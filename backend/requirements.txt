# Web API framework and local development server.
fastapi==0.139.0
uvicorn==0.51.0

# PostgreSQL ORM and bundled PostgreSQL driver (includes libpq on Windows).
SQLAlchemy==2.0.51
psycopg[binary]==3.3.4

# Request validation, email validation, and environment-variable loading.
pydantic==2.13.4
email-validator==2.3.0
python-dotenv==1.2.2

# Authentication: JWT access/reset tokens and Argon2/bcrypt password hashes.
python-jose==3.5.0
passlib==1.7.4
argon2-cffi>=23.1.0
bcrypt==4.2.1

# Multipart file uploads plus PDF reading and DOCX-to-PDF preview generation.
python-multipart==0.0.32
pypdf>=5.0
reportlab==5.0.0

# Groq LLM HTTP client and password-reset email delivery.
httpx==0.28.1
fastapi-mail==1.5.8
