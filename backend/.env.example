# SQLite is the default for local development; this value is optional.
DATABASE_URL=sqlite:///./contractiq.db

# Replace this value before deploying the API.
SECRET_KEY=replace-with-a-long-random-secret
ALGORITHM=HS256
